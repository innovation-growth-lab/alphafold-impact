\documentclass{article}
\usepackage{adjustbox}
\usepackage{graphicx}
\usepackage{float}
\usepackage{pgffor}
\usepackage{tabularx}
\usepackage{booktabs}
\begin{document}

% Import all tabular files in the folder and add an adjustbox around each
\newcommand{\importtables}{
    \foreach \file in {../ln1p_cited_by_count, ../ln1p_fwci, ../ln1p_patent_count, ../ln1p_patent_citation, ../ln1p_ca_count, ../pdb_submission} { % Replace with actual file names
        \begin{adjustbox}{width=1\textwidth}
            \input{\file}\\
        \end{adjustbox}
    }
}

\importtables

\end{document}